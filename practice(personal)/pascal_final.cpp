#include<iostream>
using namespace std;

int main()
{
    int n,v;  //initialised no. of rows and initial value of the triangle;
    cout<<"Enter the number of rows:";
    cin>>n;  //took the input iof rows

    for (int rows = 0; rows < n; rows++)
    {
        for (int space = 1; space < n-rows; space++)
        {
            cout<<" "
        }
        
        for (int values = 0; values < count; i++)
        {
            /* code */
        }
        
    }
    
    return 0;
}